server:
  port: ${sp.doc}
spring:
  application:
    name: ${sn.doc}
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted # 全局逻辑删除字段名(可选)
      logic-delete-value: 1       # 逻辑已删除值(默认就是1)
      logic-not-delete-value: 0   # 逻辑未删除值(默认就是0)