
server:
  port: ${sp.stumanager}
spring:
  application:
    name: ${sn.stumanager}
  jackson:
    date-format: yyyy-MM-dd
    time-format: HH:mm
    serialization:
      write-dates-as-timestamps: false

mybatis:
  mapper-locations: classpath:/mapper/*.xml
