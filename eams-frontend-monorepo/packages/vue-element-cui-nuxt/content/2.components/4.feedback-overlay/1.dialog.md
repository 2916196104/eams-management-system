---
title: Dialog
description: `CuiDialog` 负责确认、提示和轻量补充信息场景，不应承载过长或过重的流程。
---

# CuiDialog

`CuiDialog` 负责确认、提示和轻量补充信息场景，不应承载过长或过重的流程。

## ::demo-playground

title: 基础确认弹层
description: 演示标题、正文和确认取消动作的基础组合

---

#preview
<DialogOverviewDemo />

#code

```vue
<script setup lang="ts">
import DialogOverviewDemo from "~/app/components/demo/DialogOverviewDemo.vue";
</script>

<template>
	<DialogOverviewDemo />
</template>
```

::

::api-table{title="核心 Props" description="确认弹层最常见配置"}
| 属性 | 类型 | 说明 |
| --- | --- | --- |
| `modelValue` | `boolean` | 是否打开 |
| `title` | `string` | 标题 |
| `confirmText` | `string` | 确认按钮文案 |
| `cancelText` | `string` | 取消按钮文案 |
| `loading` | `boolean` | 是否展示确认加载状态 |
::
